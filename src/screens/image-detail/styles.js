import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imgMain: {
    height: 380,
    backgroundColor: '#ececec',
  },

  viewInfo: {
    padding: 14,
  },
  txtInfo: {
    lineHeight: 20,
  },
});
